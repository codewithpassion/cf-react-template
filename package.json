{
  "name": "demo",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@torchauth/vite-plugin-wrangler-spa": "^2.0.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "scripts": {
    "dev": "bunx vite --mode page-function",
    "build": "vite build &&  vite build --mode page-function"
  }
}