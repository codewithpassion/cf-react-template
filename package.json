{
  "name": "cf-react-template",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.14",
    "@types/bun": "latest",
    "tailwindcss-animate": "^1.0.7"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "cd frontend && bunx vite",
    "build": "cd frontend && tsc && bunx vite build",
    "preview": "cd frontend && bunx vite preview"
  },
  "dependencies": {
    "react-markdown": "^9.0.1"
  }
}